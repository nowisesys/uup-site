<!--
<?php
include_once('format.inc');
$output = new format($this);
$config = $this->config;
?>
-->
<!DOCTYPE html>
<html lang="en">
    <head>
        <title><?= $output->title() ?></title>

        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <?php $output->headers(); ?>

        <link href="<?= $config->getCss('bootstrap.min.css')  ?>" rel="stylesheet">
        <link href="<?= $config->getCss('custom.css') ?>" rel="stylesheet">        

        <script src="<?= $config->getJs('jquery.min.js') ?>"></script>
        <script src="<?= $config->getJs('bootstrap.min.js') ?>"></script>

        <script>
            // 
            // Keep track of selected topmenu item:
            // 
            $(document).ready(function () {
                $("#" + localStorage.getItem('menu')).addClass("active");

                $(".topmenu").on('click', function () {
                    $("#" + localStorage.getItem('menu')).removeClass("active");
                    $(this).addClass("active");
                    localStorage.setItem('menu', $(this).attr("id"));
                })
            });
        </script>
    </head>
    <body>

        <nav class="navbar navbar-inverse">
            <div class="container-fluid">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>                        
                    </button>
                    <a class="navbar-brand" href="#">The Company</a>
                </div>
                <div class="collapse navbar-collapse" id="myNavbar">
                    <ul class="nav navbar-nav">
                        <?php $output->topmenu(); ?>
                    </ul>
                    <ul class="nav navbar-nav navbar-right">
                        <li><a href="#"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
                        <li><a href="#"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
                    </ul>
                </div>
            </div>
        </nav>

        <div class="container-fluid text-center">    
            <div class="row content">
                <div class="col-sm-2 sidenav">
                    <?php $output->navmenu(); ?>
                </div>
                <div class="col-sm-8 text-left"> 
                    <?php $output->content(); ?>                            
                </div>
                <div class="col-sm-2 sidenav">
                    <?php $output->sidemenu(); ?>
                </div>
            </div>
        </div>

        <footer class="container-fluid text-center">
            <?php $output->footer(); ?>
        </footer>

    </body>
</html>
